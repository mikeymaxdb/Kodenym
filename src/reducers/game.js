// import * as types from 'constants/actionTypes'

const initialState = {
    name: '',
}

const game = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default game
